export type user = {
   cep: string
   logradouro: string
   número: string
   complemento?: string
   bairro: string
   cidade: string
   estado: string
   id: string
   name: string
   nickname: string
   email: string
}

export type addressInfo = {
   street: string,
   neighborhood: string,
   city: string,
   state: string
}